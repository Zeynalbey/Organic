@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@using Organic.Areas.Admin.ViewModels.Slider
@model List<IndexViewModel>

@foreach (var item in Model)
{
    <li>
        <div class="slide-contain slider-opt03__layout01">
            <div class="media">
                <div class="child-elememt">
                    <a href="#" class="link-to">
                        <img src="~/@item.Image" width="604" height="580" alt="">
                    </a>
                </div>
            </div>
            <div class="text-content">
                <i class="first-line">@item.ProductName</i>
                <h3 class="second-line">@item.Title</h3>
                <p class="third-line">@item.Description</p>
                <p class="buttons">
                    <a asp-route="client-basket-add" asp-route-id="@item.ProductId" class="btn btn-bold add-basket-btn">Səbətə əlavə et</a>

                    <a asp-route="client-product-detail" asp-route-id="@item.ProductId" class="btn btn-thin">Detallara bax</a>
                </p>
            </div>
        </div>
    </li>
}
